import React from 'react';
import { Box } from 'theme-ui';

const Story = () => {
  return (
    <Box
      sx={{
        width: '100%',
        bg: '#fff',
        height: '100px',
        paddingX: '10px',
        marginBottom: '30px',
      }}
    >
      This is a story block
    </Box>
  );
};

export default Story;
